# RTX 5070 Max-Q has no display outputs — all monitors are wired through Intel iGPU.
# NVIDIA is available for render offload (PRIME) only.
# HDR on external displays depends on i915 MST support for HDR_OUTPUT_METADATA.
