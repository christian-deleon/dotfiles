# Change the default Omarchy look'n'feel

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    # No gaps between windows or borders
    gaps_in = 2
    gaps_out = 2
    # border_size = 0

    # Use master layout instead of dwindle
    # layout = master
}

# Omarchy default sets focus_on_activate = true. That steals focus whenever a
# window requests activation — e.g. agent terminals when a turn finishes and
# notify-send / OSC notifications fire. Keep toasts/bells; don't jump windows.
# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    focus_on_activate = false
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    # Use round window corners
    # rounding = 8
}

# https://wiki.hypr.land/Configuring/Variables/#layout
layout {
    # Avoid overly wide single-window layouts on wide screens
    # single_window_aspect_ratio = 1 1
}
