# See https://wiki.hyprland.org/Configuring/Monitors/
# List current monitors and resolutions possible: hyprctl monitors
# Format: monitor = [port], resolution, position, scale

# Laptop display: ThinkPad T1g Gen 8 — Samsung OLED 3200x2000 120Hz
monitor = eDP-1, 3200x2000@120, auto, 1.33, bitdepth, 10, cm, hdr

# Desk monitors: triple MSI MAG 321UP OLEDs (side by side, 4K@120 via 2x DP 2.1 + 1x TB5)
monitor = DP-4, 3840x2160@120, 0x0, 1, bitdepth, 10
monitor = DP-10, 3840x2160@120, 3840x0, 1, bitdepth, 10
monitor = DP-8, 3840x2160@120, 7680x0, 1, bitdepth, 10

# Portable ViewSonic VX1655-OLED (overrides DP-1 when connected)
monitor = desc:ViewSonic Corporation VX1655-OLED, 3840x2160@60, auto, 1.5

# Fallback for any other monitors
env = GDK_SCALE,1
monitor=,preferred,auto,1

# Good compromise for 27" or 32" 4K monitors (but fractional!)
# env = GDK_SCALE,1.75
# monitor=,preferred,auto,1.6

# Straight 1x setup for low-resolution displays like 1080p or 1440p
# env = GDK_SCALE,1
# monitor=,preferred,auto,1

# Example for Framework 13 w/ 6K XDR Apple display
# monitor = DP-5, 6016x3384@60, auto, 2
# monitor = eDP-1, 2880x1920@120, auto, 2
