# Empty alacritty theme (last-resort fallback).
# Prefer the Everforest colors at:
#   omarchy/.config/omarchy/current/theme/alacritty.toml
# alacritty_setup links that into ~/.config/omarchy/current/theme/alacritty.toml
# on non-Omarchy hosts; this file is only used if that package path is missing.
