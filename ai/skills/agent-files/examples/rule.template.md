Always prefer `tofu` over `terraform` in command examples.
Never commit `.env`, `credentials.json`, `secrets.yaml`, or any file with `secret` in its name.
When unsure whether a command is destructive, ask before running it.

When suggesting Kubernetes manifests, target API versions current as of 1.30+.
